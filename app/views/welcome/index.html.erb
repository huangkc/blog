<% if logged_in? %>
  <div class="row">
        <aside class="col-md-4">
          <section class="user_info">
            <%= render 'shared/user_info' %>
          </section>
          <section class="micropost_form">
            <%= render 'shared/micropost_form' %>
          </section>
        </aside>

        <div class="col-md-8">
          <h3>MicroBlog Post Feed</h3>
          <%= render 'shared/feed' %>
        </div>
  </div>

<% else %>

<div class="row">
  <div class="jumbotron">
     <div class="container">

      <p>MicroBlog</p>
      <p><%= link_to "GET STARTED TODAY", signup_path, class: "btn btn-lg btn-primary" %>
<!--         <%= link_to "Log in", login_path, class: "btn btn-lg btn-primary" %></p>
 -->
<!--         <p><a class="btn btn-primary btn-lg" href="#" role="button">Find out more! &raquo;</a></p> -->
      </div>
  </div>
</div>

<% end %>

